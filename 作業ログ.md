# 作業ログ

## 2026-09-26

- アイコンを作り直し（白地に大きな日の丸、「代表」は小さめ・「TV」は少し大きめ）。公開済み
  - 9/23に別タブが日の丸を入れていたが、このPCの手元に取り込まれておらず、iPhoneに載っていなかった
- 結果（スコア）の反映漏れを修正
  - 9/23日本×タイ(U-21、アジア大会)は取得元の日程から消えており、matches.jsonからも消えていた。
    NHK・中日新聞の報道で3-0を確認し手で復元
  - 原因：9/23の`c2116e2`の対策は「日程に残っている試合の放送局情報が空になる」ことだけを防いでおり、
    試合そのものが日程から消える場合は対象外だった
  - 対策：`carry_over_finished()`を追加。取得元から消えた**過去の**試合を（年・日付・時刻）をキーに
    既存データから引き継ぐ。未来の試合は中止・変更の可能性があるので引き継がない。テスト4件追加（計55件パス）
- 続けて、スコア取得元を追加（本人の指摘「他に試合結果のページくらいあるべさ」）
  - 若い世代の一覧（goal.comの今日のサッカー予定）にはスコア欄がなく、U-21の試合は結果が入らなかった
  - JFA公式の「スケジュール・結果」ページ（SAMURAI BLUE／なでしこ／U-21、年別）は静的な表で取れる。
    `apply_official_results()`で、スコアが空の**過去の**試合に日付で突き合わせて反映する（既存スコアは上書きしない）。
    JFAは日本が先のスコアなので、日本がteam2の試合はhome/awayを入れ替える。テスト2件追加（計57件パス）
  - 実地確認：9/23のスコアを空にして更新→「公式結果を反映: 9/23 日本 3-0 タイ」
  - 限界：U-21以外の若い世代（U-19、U-23など）はJFAの別ページなので未対応（必要になったら追加）

## 2026-09-23

- なでしこジャパン（女子A代表）・若い世代（U-21等）のTV放送予定に対応
  - データソース: なでしこは専用の日程・放送記事ページ、若い世代はgoal.comの
    ローリング「今日のサッカー予定」ページ（6日先まで）を採用。JFA公式・個人ブログ等
    も調べたが、若い世代は対戦相手・放送局とも直前まで決まらないことが多く、
    これ以上の先読みは難しいと判断（大型大会のときはJFAの大会別放送ページを
    都度手動追加する運用でカバー）
  - matches.jsonに`team`フィールド（a/nadeshiko/youth）を追加、既存データもマイグレーション
  - index.htmlに team区分バッジを追加（1つの一覧に日付順で混在表示）
  - 実装はOpusモデルのAgentに委任、完了後に動作確認・テスト確認（51件パス）
- ホーム画面アイコン(icon.png)の中央に日の丸を追加（当初は右上バッジ案だったが
  「ダサい」とのことで中央配置に変更）
- **注意**: このフォルダは別リポジトリ(`japan-national-team-tv-checker`)のgit submodule。
  作業中に気づかずdetached HEAD状態でcommitしてしまい、submoduleのmainブランチが
  （既存のscraper.pyバグ修正コミット分）先に進んでいたのを見落としかけた。
  `git merge`で統合し、JSON自動マージで発生した9/24ウルグアイ戦の重複エントリは
  手動で解消済み。submoduleへのcommitは完了、親リポジトリ(claude-workspace)側の
  サブモジュール参照更新commitも完了。**pushは未実施**（隣のタブで実施予定）

## 止まっていること

- なし（このセッションでの作業は完了、pushのみ別タブで実施予定）
